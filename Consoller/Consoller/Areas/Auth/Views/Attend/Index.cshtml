@model Consoller.Areas.Auth.Models.MorningAttendence
@using Consoller.Areas.Auth.Models
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Auth/Views/Shared/Layout.cshtml";
    Attendence at = new Attendence();

}

<div class="table-responsive">

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">
       
            <div class="form-group">
               
                <div class="col-md-10">
                    <input type="date" class="form-control" name="date" />
                  
                </div>
            </div>

        </div>
    }

    @if (Model != null)
    {
        <form action="/Attend/Create" method="post">
            <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                <thead>
                    <tr>
                        <th>
                            RollNo
                        </th>
                        <th>
                            Name
                        </th>
                        <th>
                            Morning
                        </th>



                    </tr>
                </thead>
                @*@foreach (var item in Model)
                    {

                        <tr>
                            <td>
                                <input type="hidden" value="@item.rollno" name="Roll" />
                                @Html.DisplayFor(modelItem => item.rollno)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.name)
                            </td>
                            <td>
                                @{ int a = Convert.ToInt32(item.Id) - 1;}
                              P  <input type="radio" name="Reading[@a]" value="Present">
                              A  <input type="radio" name="Reading[@a]" value="Absent">





                            </td>


                        </tr>
                    }*@

            </table>

            <input type="submit" name="submit" value="submit" /><br>
        </form>
    }

    
</div>



